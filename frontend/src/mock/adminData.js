// Deprecated: retained for backward compatibility, re-exporting the consolidated mock data.
// Prefer importing from `src/mock/appData` directly.

export {
  tourists as mockTourists,
  alerts as mockAlerts,
  responseUnits as mockUnits,
  getAuthorityStats as getMockStats,
  formatTime
} from './appData';
