# SafarSathi Spring Boot Backend
spring.application.name=safarsathi-backend

# Server
server.port=${PORT:8081}

# MongoDB
spring.data.mongodb.uri=${MONGO_URI:mongodb+srv://manjeet:Manjeet70%40@cluster0.mlc7b7t.mongodb.net/safarsathi?retryWrites=true&w=majority&appName=Cluster0}
spring.data.mongodb.database=safarsathi

# JWT
app.jwt.secret=${JWT_SECRET:default_unsafe_secret_key_for_development_only_change_in_production}
app.jwt.expiration-ms=${JWT_EXPIRY_MS:604800000}

# CORS
app.cors.allowed-origins=${CORS_ORIGIN:*}

# Logging
logging.level.com.safarsathi=INFO
logging.level.org.springframework.security=WARN
